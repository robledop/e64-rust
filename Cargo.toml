[workspace]
members = ["xtask"]
resolver = "2"

[package]
name = "e64-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
font8x8 = { version = "0.3", default-features = false }
limine = "0.5"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
debug = true
